services:
  ca-clusters-api:
    image: chain-alpha-python:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chain-alpha-ca-clusters-api
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      TZ: Asia/Shanghai
      CHAIN_ALPHA_REDIS_ENABLED: "1"
      CHAIN_ALPHA_PLUGIN_API_KEY: "${CHAIN_ALPHA_PLUGIN_API_KEY:-}"
      PLUGIN_SIGNAL_STREAM_MAX_AGE_SEC: "3600"
    ports:
      - "8012:8089"
    command:
      - uvicorn
      - web_dashboard.app:app
      - --host
      - "0.0.0.0"
      - --port
      - "8089"
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
